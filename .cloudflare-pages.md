# Cloudflare Pages 部署配置

# 构建命令
npm run build

# 输出目录
dist

# 环境变量
# 在 Cloudflare Pages 控制台设置：
# PUBLIC_API_BASE_URL = https://sbnone.dpdns.org/api
# PUBLIC_BASE_PATH = / （如果自定义域名就是 /，如果是 *.pages.dev 项目名就是 /fitness）
