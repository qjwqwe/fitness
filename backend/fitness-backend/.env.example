# 服务配置
PORT=3000
NODE_ENV=development

# JWT 密钥 (生产环境必须修改为复杂随机字符串)
JWT_SECRET=change_this_to_secure_random_string_in_production

# CORS 允许的源 (前端域名)
CORS_ORIGIN=https://fitness.sbnone.dpdns.org

# 数据库配置 (SQLite 无需额外配置)
DB_PATH=./database.sqlite
